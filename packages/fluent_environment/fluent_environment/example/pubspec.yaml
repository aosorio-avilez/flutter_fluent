name: fluent_environment_example
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: '^3.9.2'

dependencies:
  flutter:
    sdk: flutter
  fluent_environment:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
