name: fluent_networking_example
description: Demonstrates how to use the fluent_networking package.
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: '^3.9.2'

dependencies:
  flutter:
    sdk: flutter
  fluent_networking:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
